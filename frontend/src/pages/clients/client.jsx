import '../../App.css'
import { StyledClient } from './client.style'

export default function Client() {

    return(
        <StyledClient>
            <section>
            <h1>Clients</h1>
            <button>New Client + </button>
            </section>
            
        </StyledClient>

    )
}